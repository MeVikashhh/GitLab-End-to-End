apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment
  namespace: webapplication
  labels:
    app: web-application:v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-application:v1
  template:
    metadata:
      name: web-application:v1
      labels:
        app: web-application:v1
    spec:
      containers:
        - name: web-application:v1
          image: mevikash/web-application:v1
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: webapplication
spec:
  type: NodePort
  selector:
    app: web-application:v1
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 31200
